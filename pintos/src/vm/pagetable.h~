#include <stdio.h>
#include <hash.h>
#include "filesys/directory.h"
#include "filesys/file.h"
#include "filesys/filesys.h"
#include "vm/page.h"

/* type of the page table. */
enum pagetable_type
  {
    TABLE_FRAME,	        /* frame table, map PFN to actual
				   physical address*/
    TABLE_PAGE,			/* page table */
    TABLE_SWAP,
    TABLE_FILE_MAPPING
  }


struct pagetable{
  
}
